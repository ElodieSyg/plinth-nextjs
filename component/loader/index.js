import ReactlLoading from "react-loading";

const Loader = ({ type, color }) => {
    return (
        <ReactlLoading type="bubbles" color="#8fb4a0" height={100} width={100} />
    );
};

export default Loader;